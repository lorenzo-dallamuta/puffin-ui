import styled from "styled-components";
import { Section, type SectionProps } from "@radix-ui/themes";

export type StyledSectionProps = SectionProps & {};

export const StyledSection = styled(Section)``;